/// Pure-Swift core for Runner Jams: catalog models, set planning, and next-track ranking.
///
/// This module must not import MusicKit, SwiftData, or UIKit so it stays testable on macOS.
public enum RunnerJamsCore {
    public static let version = "0.1.0"
}
